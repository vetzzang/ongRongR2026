# 옹알옹알 — 스마트폰 설치(PWA) 만들기 안내

## 0. 파일 구성 (총 6개)

| 파일 | 역할 |
|---|---|
| `index.html` | 지금의 jjikjjik.html — **이름을 index.html로 변경** |
| `manifest.webmanifest` | 앱 이름·아이콘·전체화면 선언 |
| `sw.js` | 오프라인 캐시 담당 (브라우저 규칙상 별도 파일 필수) |
| `icon-192.png` `icon-512.png` `icon-512-maskable.png` | 홈 화면 아이콘 |

> 아이콘은 임시로 만든 파형 모양입니다. 나중에 캐릭터 이미지(빨간머리)로
> 같은 파일명·같은 크기로 교체만 하면 됩니다.

---

## 1. index.html에 추가할 코드 — 두 군데

### ① `<head>` 안에 (기존 meta 태그들 근처)

```html
<link rel="manifest" href="manifest.webmanifest">
<meta name="theme-color" content="#10151F">
<link rel="apple-touch-icon" href="icon-192.png">
```

### ② `</body>` 바로 앞에 (스크립트 맨 끝)

```html
<script>
/* 서비스 워커 등록 — 설치 후 오프라인 동작의 핵심 */
if ('serviceWorker' in navigator) {
  window.addEventListener('load', function () {
    navigator.serviceWorker.register('./sw.js');
  });
}
/* 저장소 보호 요청 — 저장공간 부족 시 MP3·설정이 지워지지 않게 */
if (navigator.storage && navigator.storage.persist) {
  navigator.storage.persist();
}
</script>
```

주의: 이미 비슷한 코드가 파일 안에 있다면 중복 등록하지 말고 기존 것과 비교해서 하나만 남기세요.

---

## 2. 배포 (최초 1회, HTTPS 필수)

`file://`로 연 HTML은 설치가 안 되므로 한 번은 인터넷 주소에 올려야 합니다.
6개 파일을 **같은 폴더**에 두고 아래 중 하나로:

- **Replit**: 프로젝트에 6개 파일을 넣고 Deploy (Static 배포면 충분)
- **GitHub Pages**: 저장소에 올리고 Settings → Pages 활성화 (무료·영구)

---

## 3. 폰에서 설치

1. 안드로이드 **크롬**으로 배포 주소 접속
2. 우상단 ⋮ 메뉴 → **"앱 설치"** (또는 "홈 화면에 추가")
3. 홈 화면에 옹알옹알 아이콘 생성 → 실행하면 주소창 없는 전체화면 앱

## 4. 오프라인 확인 (반드시)

1. 설치된 앱을 한 번 실행했다 닫기
2. **비행기 모드** 켜기
3. 앱 실행 → 정상 동작하면 완료

---

## 5. 이후 업데이트 방법

1. index.html 수정
2. `sw.js` 맨 위 `'ongal-v1'` → `'ongal-v2'`로 숫자 올리기 ← **잊으면 갱신 안 됨**
3. 재배포 → 폰이 온라인 상태로 앱을 열면 자동으로 새 버전 적용
   (오프라인이면 이전 버전이 계속 동작 — 멈추지 않음)
